services:
  omnidump:
    image: omnidump:latest
    init: true
    ports:
      - "5555:3000"
    volumes:
      - omnidump_data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always

volumes:
  omnidump_data: